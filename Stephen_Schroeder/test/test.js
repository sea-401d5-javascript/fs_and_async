const chai = require('chai');
const main = require('../main');

describe('readFile processes', () => {
  it('should have an 8 byte value of hex', () => {
    fs.readFile(__dirname + '/../one.txt', () => {
      expect()
    })
  })
})
